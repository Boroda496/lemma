{
  "name": "lemma-sync-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "login": "wrangler login",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
